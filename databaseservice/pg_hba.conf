# Only the image's own entrypoint and health checks reach the socket.
local   all  postgres             trust

# Anything on the network must hold a certificate from this database's CA
# whose common name is the database user. There is no password to guess.
hostssl all  postgres  0.0.0.0/0  cert
hostssl all  postgres  ::/0       cert
