kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: worker
- role: worker
- role: worker
- role: control-plane
  extraPortMappings:
  # Envoy's node port, from manifests/gateway.yaml.
  - containerPort: 30080
    hostPort: 80
    protocol: TCP
